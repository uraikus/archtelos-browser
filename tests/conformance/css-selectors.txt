# Selectors this instrument asks both engines about, one per line, run
# against tests/fixtures/selectors.html. A selector counts as
# implemented when it matches exactly the elements Chromium matches --
# not merely when it parses.
#
# The list is Selectors 3's own feature set plus every part of
# Selectors 4 this fixture can grade without being changed, so the count
# says what is left rather than only what works. The number that passes
# may not go down (CLAUDE.md §2); adding a row that fails is how the
# denominator grows, and is the point.
#
# A selector that matches nothing in the fixture proves nothing: an
# engine that drops it agrees with one that implements it. Every
# selector here matches at least one element in Chromium, and
# tests/run.sh fails the run if that stops being true.
#
# A blank line is skipped, and so is a comment -- which is "# " or a
# bare "#", with the space required, because "#p1" is an id selector.

# --- Selectors 3: simple selectors
*
p
#p1
.lead
.lead.tail
p.lead
div p
div > p
h2 + p
h2 ~ ul
p, span

# --- Selectors 3: attribute selectors
[data-kind]
[data-kind="alpha"]
[data-kind~="alpha"]
[data-kind|="alpha"]
[data-kind^="alpha"]
[data-kind$="beta"]
[data-kind*="pha"]
[title]

# --- Selectors 3: structural pseudo-classes
:root
p:first-child
p:last-child
span:only-child
li:nth-child(1)
li:nth-child(odd)
li:nth-child(even)
li:nth-child(2n)
li:nth-child(2n+1)
li:nth-child(3n+2)
li:nth-child(-n+3)
li:nth-child(n)
li:nth-child(n+3)
li:nth-last-child(1)
li:nth-last-child(2)
li:nth-last-child(odd)
li:nth-last-child(2n+1)
p:first-of-type
p:last-of-type
p:only-of-type
p:nth-of-type(2)
p:nth-of-type(2n)
p:nth-last-of-type(1)
p:nth-last-of-type(2n+1)
:empty
p:empty
div:empty

# --- Selectors 3: other pseudo-classes
a:link
:lang(fr)
input:enabled
input:disabled
input:checked
option:checked

# --- Selectors 3: negation
p:not(.lead)
div:not(.box)
li:not(:first-child)
:not(p)

# --- Selectors 4, in the snapshot's lowest class
:is(h2, span)
:where(h2, span)
div:has(.lead)
li:not(:first-child, :last-child)
[data-kind="ALPHA" i]

# --- Selectors 4: the link and form-state pseudo-classes
:any-link
a:any-link
input:read-write
p:read-only
input:optional
input:default
option:default
input:valid

# --- Selectors 4: :dir()
p:dir(ltr)

# --- Selectors 4: a complex selector inside :is(), :where(), :not()
# and :has(). Each takes a <complex-selector-list> in §3.1, not a
# compound, and :has() takes a relative selector -- so a leading
# combinator in it names a child or a sibling rather than a descendant.
:is(div > p)
:where(div > p)
p:not(div > p)
:is(h2):is(#h1)
:not(:not(p))
div:has(> p)
div:has(+ div)
p:has(+ p)
div:not(:has(p))

# --- Selectors 4: :nth-child()'s `of S` clause, which filters which
# siblings are counted before An+B is applied to them
p:nth-child(2 of .lead)
:nth-child(1 of p)
li:nth-child(even of :not(.x))

# --- Selectors 4: combinations the list did not otherwise reach
*|p
:root > body
:checked + input
input:enabled:checked
